interface:
  display_name: "Arena"
  short_description: "Compare isolated alternatives under parent judgment"
  default_prompt: "Use $arena for this request."
policy:
  allow_implicit_invocation: false
