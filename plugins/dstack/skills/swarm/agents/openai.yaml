interface:
  display_name: "Swarm"
  short_description: "Coordinate independent execution and exploration batches"
  default_prompt: "Use $swarm for this request."
policy:
  allow_implicit_invocation: false
