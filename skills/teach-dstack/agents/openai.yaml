interface:
  display_name: "Teach"
  short_description: "Explain a body of work plainly so a person actually understands it. Runs the `how-dstack` and `why-dstack`..."
  default_prompt: "Use $teach-dstack for this request."
policy:
  allow_implicit_invocation: false
