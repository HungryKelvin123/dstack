# Hillclimb

Use for repeated improvement against one metric. Fix the workload and baseline, keep one decision row per attempt, change one hypothesis at a time, retain only measured wins that preserve behavior, and stop at the user's target or an evidence-backed plateau. Runtime measurements that require Studio are user-run. Never substitute `rojo build` for a performance measurement.
