# Eval

Compare instruction or workflow changes on representative Roblox requests. Keep the task, repository fixture, model, effort, and success criteria stable. Measure correctness, security boundaries, completeness, total tokens, tool calls, and latency. Promote a change only when the final result improves without weakening required behavior.
