# Prototype

Use a disposable prototype to answer one empirical design question. Keep it outside production paths, name the deciding observation, and compare only the minimum alternatives needed. Do not use a Studio playtest through MCP. Preserve the result as evidence, then delete or clearly isolate prototype code before implementing the chosen design.
